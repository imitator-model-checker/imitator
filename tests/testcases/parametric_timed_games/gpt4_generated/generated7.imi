(* Automatically generated by GPT4 
    Expected constraint: 
    4 > p
    & p >= 1
*)
var
    x : clock;
    y : clock;
    p : parameter;

automaton ptg

actions: c_1, u_1;

loc l1: x <= 20 & y <= 10
    when x >= p && y <= 6 do {y := 0} sync c_1 goto Goal;
    when y >= 4 sync u_1 goto l2;

loc l2: invariant x <= y + 7

accepting loc Goal: invariant x <= 18

end

init :=
    & loc[ptg] = l1
    & x = 0
    & y = 0
    & p >= 1
;

end
