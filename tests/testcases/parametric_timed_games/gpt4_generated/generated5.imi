(* Automatically generated by GPT4 
    Expected constraint: 
    5 >= p
    & p >= 1
*)
controllable actions: c_1;

var
    x : clock;
    y : clock;
    p : parameter;

automaton ptg

actions: c_1, u_1;

loc l1: x <= 20 & y <= 10
    when x >= p && y <= 5 do {y := 0} sync c_1 goto Goal;
    when x >= 8 sync u_1 goto l2;

loc l2: invariant x >= y + 3

accepting loc Goal: invariant x <= 15

end

init :=
    & loc[ptg] = l1
    & x = 0
    & y = 0
    & p >= 1
;

end
