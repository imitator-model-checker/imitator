(library
 (name exporter)
 (libraries utils entities parsing lib gmp ppl extlib fileutils str unix threads)
 (flags
  (:standard -open Utils -open Entities -open Parsing -open Lib))
 (modules_without_implementation
  modelExporter))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
