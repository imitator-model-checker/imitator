(library
 (name lib)
 (libraries data_structures utils entities parsing gmp ppl extlib fileutils str unix threads)
 (flags
  (:standard -open Utils -open Entities -open Parsing -open Data_structures))
 (modules_without_implementation result))

(ocamllex
 (modules  ModelUpdateLexer))

(rule
 (targets ModelUpdateParser.conflicts)
 (deps ModelUpdateParser.mly)
 (action
  (run menhir --dump --explain %{deps})))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
