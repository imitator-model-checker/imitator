(library
 (name lib)
 (libraries utils entities parsing gmp ppl extlib fileutils str unix threads)
 (flags
  (:standard -open Utils -open Entities -open Parsing))
 (modules_without_implementation result))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
