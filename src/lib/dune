(library
 (name lib)
 (libraries parsing gmp ppl extlib fileutils str unix threads)
 (flags
  (:standard -open Parsing))
 (modules_without_implementation
  abstractModel
  abstractProperty
  result))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
